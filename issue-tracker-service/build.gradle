repositories {
    mavenCentral()
}

dependencies {
    compile project(':issue-tracker-repository')

    testCompile "junit:junit:${jUnitVersion}"
    testCompile "org.easymock:easymock:${easyMockVersion}"
}
