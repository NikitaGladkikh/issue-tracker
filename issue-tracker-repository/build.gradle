apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':issue-tracker-domain')
    compile("org.mybatis:mybatis:${myBatisVersion}")
    compile("postgresql:postgresql:${postgreSQLVersion}")
}
